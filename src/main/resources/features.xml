<?xml version="1.0" encoding="UTF-8"?>
<features>

	<!-- local features repository -->
	<repository>mvn:${project.groupId}/${project.artifactId}/${project.version}/xml/features</repository>
	<!-- Apache CXF features repository -->
	<repository>mvn:org.apache.cxf.karaf/apache-cxf/${cxf-version}/xml/features</repository>

	<feature name="${project.groupId}.${project.artifactId}" version="${project.version}">
		<!-- dependencies features -->		
		<feature version="${cxf-version}">cxf</feature>
		
		<!-- runtime dependencies -->
		<bundle dependency="true">mvn:org.osgi/org.osgi.core/${osgi.core-version}</bundle>
		<bundle dependency="true">mvn:com.google.guava/guava/${guava-version}</bundle>
		<bundle dependency="true">mvn:org.apache.commons/commons-lang3/${commons-lang3}</bundle>
		<bundle dependency="true">mvn:org.apache.commons/commons-collections4/${commons-collections4-version}</bundle>
		
		<bundle>mvn:org.opennaas/mqnaas.core/${project.version}</bundle>
	</feature>

</features>